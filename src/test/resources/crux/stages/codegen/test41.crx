// test nested (boolean) expression

var a int;
var b [1]int;


func foo() bool {
  return false;
}


func  main() {
  a = 0;
  b[0] = 1;
  var c bool;
  if (((((true))))) {
     c = (1 > (b[0] - a)) || (foo() || (((true))));
  }
}
