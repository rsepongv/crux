// test functions with value must have return

func validReturn1(a int) int {
  if (true) {
    if (false) {
      a = 3;
    } else {
      loop {
        return a;
      }
    }
    return 4;
  } else {
    return a;
  }
}

func main() {
  printInt(3);
  printInt(validReturn1(6));
}
